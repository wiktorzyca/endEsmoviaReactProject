//I import the hook createContext, mandatory to create it.
import { createContext } from 'react';

//I make my context available on the whole app to be imported.
export const myContext = createContext(null);